# Локальный override: порт 5432 на хосте часто занят другими проектами,
# поэтому наш PostgreSQL наружу отдаётся на 5433. Для локальной демо-базы
# включён trust-доступ без пароля (данные не покидают машину).
services:
  postgres:
    ports: !override
      - "5433:5432"
    environment:
