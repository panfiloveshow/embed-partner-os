-- Встроенная аутентификация по паролю: режим входа без внешнего SSO.
-- Хеш пароля в формате scrypt$<salt>$<hash>. Null означает, что вход
-- по паролю для учётной записи запрещён (пользователь только через SSO).
ALTER TABLE "user_account" ADD COLUMN "password_hash" TEXT;
